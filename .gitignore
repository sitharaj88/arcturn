node_modules/
dist/
*.tsbuildinfo

# Next.js (web/) build output — regenerate with `pnpm --filter arcturn-web build`
web/.next/
web/out/
next-env.d.ts
.DS_Store
.env
.env.*
coverage/
.pnpm-store/

# Eval run artifacts (real API runs; regenerate with `arcturn-evals run`)
runs/
*.vsix

# VS Code integration-test harness (editors/vscode): downloaded editors,
# isolated profiles, generated fixture workspaces, and the compiled test files.
.vscode-test/
editors/vscode/out/

# Agent worktrees the harness parks inside the checkout
.claude/worktrees/
