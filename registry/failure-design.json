{
  "name": "failure-design",
  "kinds": ["org-kit", "agents", "workflows", "skills"],
  "source": "sitharaj88/arcturn/kits/failure-design",
  "description": "What happens when a dependency answers slowly. Retry safety is decided by an oracle that runs the operation twice — including the timeout case where the first attempt already committed — never by a reading.",
  "maintainer": {
    "name": "Sitharaj Seenivasan",
    "url": "https://sitharaj.in"
  },
  "disclosure": {
    "agents": [
      {
        "name": "dependency-cartographer",
        "lane": "read",
        "tools": ["read", "grep", "glob", "ls", "search_code"]
      },
      {
        "name": "failure-critic",
        "lane": "read",
        "tools": ["read", "grep", "glob", "ls", "search_code"]
      },
      {
        "name": "idempotency-oracle",
        "lane": "exec",
        "tools": ["read", "grep", "glob", "ls", "bash"]
      },
      {
        "name": "resilience-author",
        "lane": "write",
        "tools": ["read", "grep", "glob", "ls", "search_code", "write", "edit"]
      }
    ],
    "workflows": [
      {
        "name": "failure-review",
        "stages": 4,
        "budgetUsd": 28
      }
    ],
    "skills": [
      {
        "name": "consistency-check",
        "line": "Find dual writes, transaction boundaries that end earlier than the code assumes, and read-after-write races the storage does not guarantee."
      },
      {
        "name": "failure-modes",
        "line": "Work every dependency through down, slow, wrong and partial, saying what actually happens at a path:line rather than that it is handled."
      },
      {
        "name": "retry-audit",
        "line": "Find every retry, multiply the attempts across all layers, and report whether the operation underneath is safe to run twice."
      },
      {
        "name": "timeout-budget",
        "line": "Trace the timeout chain hop by hop and find where it is absent, inverted, unbudgeted, or starving a shared pool."
      }
    ],
    "executable": false
  }
}
